# Rename to .env and fill in. NEVER commit .env.
# Used only for local development (demo.html dev mode).
# In production the key lives only in the Cloudflare Worker secret store.

OPENROUTER_KEY=sk-or-v1-REPLACE_ME
