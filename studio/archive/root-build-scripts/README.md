# Archived Root Build Scripts

These tracked root scripts were preserved during the root consolidation instead of being deleted:

- `build_apothecary_deck.py`
- `build_funding_sheet.py`
- `build_positioning_deck.py`

The old tracked gitlink `--help/claude-talk-to-figma-mcp` pointed at commit `d336452b408f4a20ee0803b609e7e743738bb76c`. It was an accidental duplicate path; the usable Figma MCP workspace is preserved at `studio/tools/claude-talk-to-figma-mcp/`.
